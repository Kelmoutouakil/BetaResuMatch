
services:
  app:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    container_name: my_app
    env_file: .env
    ports:
      - "443:443"
      - "5432:5432"
    volumes:
      - ./backend:/backend
